import React from "react";
import { View, Text, FlatList } from "react-native";
import styles from "../styles";

export default function telaMetas({ navigation }) {
  return (
    <View style={styles.container}>
      <Text>Tela da Metas</Text>
    </View>
  );
}

